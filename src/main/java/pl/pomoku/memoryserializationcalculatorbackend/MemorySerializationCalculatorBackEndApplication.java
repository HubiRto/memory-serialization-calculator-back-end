package pl.pomoku.memoryserializationcalculatorbackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MemorySerializationCalculatorBackEndApplication {

	public static void main(String[] args) {
		SpringApplication.run(MemorySerializationCalculatorBackEndApplication.class, args);
	}

}
